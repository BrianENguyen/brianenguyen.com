const suffix = '| BEN';

const HomePageTitle = 'Brian E. Nguyen | Frontend Developer';

const ResumePageTitle = `Resume ${suffix}`;

const PortfolioPageTitle = `Portfolio ${suffix}`;

const AboutPageTitle = `About ${suffix}`;

const ContactPageTitle = `Contact ${suffix}`;

const ErrorPageTitle = `404 ${suffix}`;

const PrivacyPageTitle = `Privacy Policy ${suffix}`;

const FirstSitePageTitle = `My First Website ${suffix}`;

const FrontendMentorPageTitle = `Frontend Mentor Challenges ${suffix}`;

const KrakenBracketPageTitle = `Kraken Bracket ${suffix}`;

const LBTCPageTitle = `CSULB Tzu Ching ${suffix}`;

const NewSitePageTitle = `My New Website ${suffix}`;

const ProbAndStatsPageTitle = `Probability and Stats ${suffix}`;

const SIMDPageTitle = `SIMD ${suffix}`;

const ThuPageTitle = `Thu Nguyen ${suffix}`;

const YelpCampPageTitle = `YelpCamp ${suffix}`;

const AllEliteNetworkPageTitle = `All Elite Network | ${suffix}`;

const BlogPageTitle = `Blog ${suffix}`;

const WhyBlogPageTitle = `Why I'm Blogging ${suffix}`;

const MyProgrammingHistoryPageTitle = `My Programming History ${suffix}`;

const VolunteeringTCPageTitle = `Volunteering with Tzu Chi ${suffix}`;

const OptimizingPageTitle = `Optimizing Website Images ${suffix}`;

export {
  HomePageTitle,
  ResumePageTitle,
  PortfolioPageTitle,
  AboutPageTitle,
  BlogPageTitle,
  ContactPageTitle,
  ErrorPageTitle,
  PrivacyPageTitle,
  FirstSitePageTitle,
  FrontendMentorPageTitle,
  KrakenBracketPageTitle,
  LBTCPageTitle,
  NewSitePageTitle,
  ProbAndStatsPageTitle,
  SIMDPageTitle,
  ThuPageTitle,
  YelpCampPageTitle,
  AllEliteNetworkPageTitle,
  WhyBlogPageTitle,
  MyProgrammingHistoryPageTitle,
  VolunteeringTCPageTitle,
  OptimizingPageTitle,
};
